<Window x:Class="TimeTask.SmartQuadrantSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TimeTask"
        Title="{local:Loc Key=SmartQuadrant_Title}" Height="520" Width="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style x:Key="QuadrantButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F8FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" 
                   Text="{local:Loc Key=SmartQuadrant_Header}" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="#333"
                   Margin="0,0,0,15"/>
        
        <!-- 说明 -->
        <TextBlock Grid.Row="1" 
                   Text="{local:Loc Key=SmartQuadrant_Description}" 
                   FontSize="12" 
                   Foreground="#666"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>
        
        <!-- 任务列表 -->
        <Grid Grid.Row="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="TasksPanel" ItemsSource="{Binding SubTasks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" 
                                    CornerRadius="8" 
                                    Padding="15"
                                    Margin="0,0,0,10"
                                    BorderBrush="#E0E0E0"
                                    BorderThickness="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <TextBlock Grid.Row="0" 
                                               Text="{Binding TaskDescription}" 
                                               FontSize="13" 
                                               FontWeight="Bold"
                                               Foreground="#333"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,10"/>
                                    
                                    <TextBlock Grid.Row="1" 
                                               Text="{Binding RecommendationText}" 
                                               FontSize="11" 
                                               Foreground="#666"
                                               Margin="0,0,0,10"/>
                                    
                                    <Grid Grid.Row="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        
                                        <Button Grid.Row="0" Grid.Column="0"
                                                Style="{StaticResource QuadrantButtonStyle}"
                                                BorderBrush="#FF4444"
                                                Click="QuadrantButton_Click"
                                                Tag="0">
                                            <StackPanel>
                                                <TextBlock Text="{local:Loc Key=Quadrant_ImportantUrgent}" FontWeight="Bold" FontSize="11"/>
                                                <TextBlock Text="{local:Loc Key=SmartQuadrant_ActionNow}" FontSize="10" Foreground="#666"/>
                                            </StackPanel>
                                        </Button>
                                        
                                        <Button Grid.Row="0" Grid.Column="1"
                                                Style="{StaticResource QuadrantButtonStyle}"
                                                BorderBrush="#4CAF50"
                                                Click="QuadrantButton_Click"
                                                Tag="1">
                                            <StackPanel>
                                                <TextBlock Text="{local:Loc Key=Quadrant_ImportantNotUrgent}" FontWeight="Bold" FontSize="11"/>
                                                <TextBlock Text="{local:Loc Key=SmartQuadrant_PlanIt}" FontSize="10" Foreground="#666"/>
                                            </StackPanel>
                                        </Button>
                                        
                                        <Button Grid.Row="1" Grid.Column="0"
                                                Style="{StaticResource QuadrantButtonStyle}"
                                                BorderBrush="#FF9800"
                                                Click="QuadrantButton_Click"
                                                Tag="2">
                                            <StackPanel>
                                                <TextBlock Text="{local:Loc Key=Quadrant_NotImportantUrgent}" FontWeight="Bold" FontSize="11"/>
                                                <TextBlock Text="{local:Loc Key=SmartQuadrant_Delegate}" FontSize="10" Foreground="#666"/>
                                            </StackPanel>
                                        </Button>
                                        
                                        <Button Grid.Row="1" Grid.Column="1"
                                                Style="{StaticResource QuadrantButtonStyle}"
                                                BorderBrush="#9E9E9E"
                                                Click="QuadrantButton_Click"
                                                Tag="3">
                                            <StackPanel>
                                                <TextBlock Text="{local:Loc Key=Quadrant_NotImportantNotUrgent}" FontWeight="Bold" FontSize="11"/>
                                                <TextBlock Text="{local:Loc Key=SmartQuadrant_Later}" FontSize="10" Foreground="#666"/>
                                            </StackPanel>
                                        </Button>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Border Background="#CCFFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="340">
                    <ProgressBar IsIndeterminate="True" Height="14" />
                    <TextBlock Text="{Binding LoadingStatus}" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="#374151" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- 底部按钮 -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button x:Name="ConfirmButton" 
                    Content="{local:Loc Key=SmartQuadrant_Confirm}" 
                    Style="{StaticResource ModernButtonStyle}"
                    Click="ConfirmButton_Click"/>
            
            <Button x:Name="CancelButton" 
                    Content="{local:Loc Key=Button_Close}" 
                    Background="#F44336"
                    Style="{StaticResource ModernButtonStyle}"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
