<Window x:Class="TimeTask.SmartQuadrantSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="智能象限分配" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style x:Key="QuadrantButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F8FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" 
                   Text="🎯 智能象限分配" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="#333"
                   Margin="0,0,0,15"/>
        
        <!-- 说明 -->
        <TextBlock Grid.Row="1" 
                   Text="请为每个子任务选择合适的象限。系统已根据任务内容进行智能推荐。" 
                   FontSize="12" 
                   Foreground="#666"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>
        
        <!-- 任务列表 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="TasksPanel" ItemsSource="{Binding SubTasks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" 
                                CornerRadius="8" 
                                Padding="15"
                                Margin="0,0,0,10"
                                BorderBrush="#E0E0E0"
                                BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- 任务描述 -->
                                <TextBlock Grid.Row="0" 
                                           Text="{Binding TaskDescription}" 
                                           FontSize="13" 
                                           FontWeight="Bold"
                                           Foreground="#333"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,10"/>
                                
                                <!-- AI推荐 -->
                                <TextBlock Grid.Row="1" 
                                           Text="{Binding RecommendationText}" 
                                           FontSize="11" 
                                           Foreground="#666"
                                           Margin="0,0,0,10"/>
                                
                                <!-- 象限选择 -->
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <Button Grid.Row="0" Grid.Column="0"
                                            Style="{StaticResource QuadrantButtonStyle}"
                                            BorderBrush="#FF4444"
                                            Click="QuadrantButton_Click"
                                            Tag="0">
                                        <StackPanel>
                                            <TextBlock Text="🔥 重要且紧急" FontWeight="Bold" FontSize="11"/>
                                            <TextBlock Text="立即处理" FontSize="10" Foreground="#666"/>
                                        </StackPanel>
                                    </Button>
                                    
                                    <Button Grid.Row="0" Grid.Column="1"
                                            Style="{StaticResource QuadrantButtonStyle}"
                                            BorderBrush="#4CAF50"
                                            Click="QuadrantButton_Click"
                                            Tag="1">
                                        <StackPanel>
                                            <TextBlock Text="📋 重要不紧急" FontWeight="Bold" FontSize="11"/>
                                            <TextBlock Text="计划安排" FontSize="10" Foreground="#666"/>
                                        </StackPanel>
                                    </Button>
                                    
                                    <Button Grid.Row="1" Grid.Column="0"
                                            Style="{StaticResource QuadrantButtonStyle}"
                                            BorderBrush="#FF9800"
                                            Click="QuadrantButton_Click"
                                            Tag="2">
                                        <StackPanel>
                                            <TextBlock Text="⚡ 不重要但紧急" FontWeight="Bold" FontSize="11"/>
                                            <TextBlock Text="委托他人" FontSize="10" Foreground="#666"/>
                                        </StackPanel>
                                    </Button>
                                    
                                    <Button Grid.Row="1" Grid.Column="1"
                                            Style="{StaticResource QuadrantButtonStyle}"
                                            BorderBrush="#9E9E9E"
                                            Click="QuadrantButton_Click"
                                            Tag="3">
                                        <StackPanel>
                                            <TextBlock Text="📱 不重要不紧急" FontWeight="Bold" FontSize="11"/>
                                            <TextBlock Text="有空再做" FontSize="10" Foreground="#666"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- 底部按钮 -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button x:Name="ConfirmButton" 
                    Content="确认分配" 
                    Style="{StaticResource ModernButtonStyle}"
                    Click="ConfirmButton_Click"/>
            
            <Button x:Name="CancelButton" 
                    Content="取消" 
                    Background="#F44336"
                    Style="{StaticResource ModernButtonStyle}"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>