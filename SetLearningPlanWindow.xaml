<Window x:Class="TimeTask.SetLearningPlanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:loc="clr-namespace:TimeTask"
        mc:Ignorable="d"
        Title="{loc:Loc SetLearningPlan_WindowTitle}" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{loc:Loc SetLearningPlan_LabelSubject}" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Name="SubjectTextBox" Grid.Row="1" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="2" Text="{loc:Loc SetLearningPlan_LabelGoal}" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Name="GoalTextBox" Grid.Row="3" Height="80" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="4" Text="{loc:Loc SetLearningPlan_LabelDuration}" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Name="DurationTextBox" Grid.Row="5" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="6" Text="{loc:Loc SetLearningPlan_LabelStartDate}" FontWeight="Bold" Margin="0,0,0,5"/>
        <DatePicker Name="StartDatePicker" Grid.Row="7" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="8" Text="{loc:Loc SetLearningPlan_Hint}" Foreground="Gray" FontStyle="Italic" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Name="SubmitButton" Content="{loc:Loc SetLearningPlan_ButtonCreate}" Width="100" Height="30" Margin="0,0,10,0" Click="SubmitButton_Click"/>
            <Button Name="CancelButton" Content="{loc:Loc Button_Cancel}" Width="80" Height="30" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
