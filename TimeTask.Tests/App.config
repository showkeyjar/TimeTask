<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <!-- If tests needed specific appSettings or connectionStrings, they'd go here -->
  <!-- For DatabaseService default constructor test, we want NO TeamTasksDbConnection string -->
  <appSettings>
    <!-- Example: Test-specific app setting if needed elsewhere -->
    <!-- <add key="TestSetting" value="TestValue"/> -->
  </appSettings>
  <connectionStrings>
    <!-- Deliberately empty or not defining TeamTasksDbConnection -->
    <!-- <add name="SomeOtherTestDb" connectionString="Data Source=test.db" /> -->
  </connectionStrings>
</configuration>
