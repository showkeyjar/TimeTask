<Window x:Class="TimeTask.SetLongTermGoalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Set Your Long-Term Goal" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Describe your long-term goal:" Grid.Row="0" Grid.Column="0" Margin="0,0,0,5"/>
        <TextBox Name="GoalDescriptionTextBox" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,0,0,10"/>

        <TextBlock Text="Enter duration (e.g., '1 year', '6 months', '90 days'):" Grid.Row="2" Grid.Column="0" Margin="0,0,0,5"/>
        <TextBox Name="DurationTextBox" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.Column="1" HorizontalAlignment="Right">
            <Button Name="SubmitButton" Content="Submit" Click="SubmitButton_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button Name="CancelButton" Content="Cancel" Click="CancelButton_Click" IsCancel="True" Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>
