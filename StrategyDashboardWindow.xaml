<Window x:Class="TimeTask.StrategyDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="策略导航面板"
        Width="980"
        Height="680"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F7F8FA">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="10" Padding="12" Margin="0,0,0,12">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="人生策略与周导航" FontSize="22" FontWeight="Bold" Foreground="#1F2937"/>
                    <TextBlock x:Name="GeneratedAtText" Text="最后更新：-" Foreground="#6B7280" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Content="刷新"
                        Width="88"
                        Height="32"
                        HorizontalAlignment="Right"
                        Click="RefreshButton_Click"
                        Background="#2563EB"
                        Foreground="White"
                        BorderBrush="#1D4ED8"/>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="12" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="用户画像" FontSize="16" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock x:Name="ProfileHeadlineText" Margin="0,8,0,6" Foreground="#374151" TextWrapping="Wrap"/>
                    <TextBlock Text="优势" FontWeight="Bold" Margin="0,8,0,4"/>
                    <ListBox x:Name="StrengthsList" Height="110"/>
                    <TextBlock Text="风险触发点" FontWeight="Bold" Margin="0,10,0,4"/>
                    <ListBox x:Name="RisksList" Height="110"/>
                    <TextBlock Text="高效时段" FontWeight="Bold" Margin="0,10,0,4"/>
                    <ListBox x:Name="PeakHoursList" Height="90"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="12" Margin="4,0,8,0">
                <StackPanel>
                    <TextBlock Text="目标层级导航" FontSize="16" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock x:Name="GoalHierarchySummaryText" Margin="0,8,0,6" Foreground="#374151" TextWrapping="Wrap"/>
                    <TextBlock Text="年度主题" FontWeight="Bold" Margin="0,8,0,4"/>
                    <ListBox x:Name="YearThemesList" Height="120"/>
                    <TextBlock Text="季度里程碑" FontWeight="Bold" Margin="0,10,0,4"/>
                    <ListBox x:Name="QuarterMilestonesList" Height="140"/>
                    <TextBlock Text="本周承诺动作" FontWeight="Bold" Margin="0,10,0,4"/>
                    <ListBox x:Name="WeekCommitmentsList" Height="90"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="12" Margin="4,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="180"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="任务取舍与周复盘" FontSize="16" FontWeight="Bold" Foreground="#111827"/>
                    <DataGrid Grid.Row="1"
                              x:Name="FocusTasksGrid"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              Margin="0,8,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="任务" Binding="{Binding TaskName}" Width="*"/>
                            <DataGridTextColumn Header="评分" Binding="{Binding Score}" Width="70"/>
                            <DataGridTextColumn Header="理由" Binding="{Binding ReasonText}" Width="170"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBlock Grid.Row="2" Text="本周策略建议" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBox Grid.Row="3"
                             x:Name="WeeklyStrategyText"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Background="#F9FAFB"
                             BorderBrush="#E5E7EB"/>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2"
                   x:Name="StatusText"
                   Margin="0,10,0,0"
                   Foreground="#6B7280"
                   Text="已加载策略数据"/>
    </Grid>
</Window>
